services:
  - type: web
    name: resume-rag-chatbot
    env: docker
    region: ohio
    plan: free
    dockerCommand: uvicorn app.main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: ALLOWED_ORIGINS
        value: "*"
